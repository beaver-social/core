import * as mediaContent from "./media";
import * as postContent from "./posts";
import * as shortContent from "./shorts";
import * as topicContent from "./topics";

// Convenience exports for common tables
export const { media } = mediaContent;
export const { posts } = postContent;
export const { shorts } = shortContent;
export const { topics } = topicContent;
