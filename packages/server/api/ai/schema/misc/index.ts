import * as awardsSchema from "./awards";
import * as timezonesSchema from "./timezones";
import * as authSchema from "./auth";

export const { awards, postAwards } = awardsSchema;
export const { timezones } = timezonesSchema;
export const { challenges } = authSchema;
